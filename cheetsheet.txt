cd (add the path of the file in your computer)
touch .gitignore
git config --global user.name Souparna
git config --global user./email youremail@example.com
git init
git add
git commit -m "Initial commit"
git status #just to see 
ssh-keygen -t rsa -b 4096 -C "youremail@example.com"
cat (paste the public key path came from above command)
git remote add origin (paste the ssh url of your github)
git push origin
